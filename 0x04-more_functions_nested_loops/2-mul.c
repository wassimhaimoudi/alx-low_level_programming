#inlude "main.h"

/**
 * mul - multiplies two integers.
 * @a: number to be multiplied.
 * @b: the factor of multiplication.
 * Return: the product of a and b.
 */
int mul(int a, int b)
{
	return (a * b);
}
