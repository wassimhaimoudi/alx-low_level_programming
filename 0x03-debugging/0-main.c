#include "main.h"
#include <stdio.h>

/**
 * main - tests the function positive_or_negative for the value of 0.
 *
 * Return: Always 0.
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);
	return (0);
}
